// Tier 2 — not bundled by default; add from Arcane when needed
// https://github.com/AXT-Studio/Arcane/tree/main/src
//

// ======== Tier 2 (add when needed; not bundled by default) ========
// GraphAdjacencyList  - graph construction / BFS / DFS
// SegmentTree         - range sum / min / max queries
// LazySegmentTree     - range update + range query
// UniqueID            - coordinate compression
// MaxFlow             - max flow problems
//
// To add: copy src/<Name>.ts from https://github.com/AXT-Studio/Arcane
// Remove import/export lines, place above this comment block.
// Respect dependency order (e.g. LazySegmentTree may need SegmentTree internals).
